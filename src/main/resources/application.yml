server:
  port: 9090

spring:
  application:
    name: shopping
  datasource:
    url: jdbc:mysql://localhost:3306/shopping
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false

logging:
  level:
    root: INFO